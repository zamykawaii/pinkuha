name: Test and Type Check

on:
  pull_request:
    branches: [main]

jobs:
  test-and-types:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
      - run: uv sync
      - run: uv run pytest
      - run: uv run pyright
